[project]
name = "typeless-reset-device"
version = "0.0.1"
description = "GUI tool for resetting Typeless device limits and migrating account data"
requires-python = ">=3.13"
dependencies = [
    "curl-cffi>=0.15.0",
    "pycryptodome>=3.23.0",
    "requests>=2.33.1",
]
